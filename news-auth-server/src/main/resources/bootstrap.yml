server:
  port: 8674
  jwt.secret: Crayon
  
spring:
  application:
    name: auth-server
  #use config center
  cloud:
    config:
      uri: http://127.0.0.1:8672
      label: master
    
